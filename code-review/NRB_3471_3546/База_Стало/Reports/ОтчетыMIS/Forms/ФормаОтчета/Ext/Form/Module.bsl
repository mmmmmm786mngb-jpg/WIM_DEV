
&НаСервере
Процедура СформироватьОтчетНаСервере()
	
	ТаблицаОтчета.Очистить();
	
	ЭтотОтчет = РеквизитФормыВЗначение("Отчет");
	ЭтотОтчет.СформироватьОтчет(ТаблицаОтчета);
	
	ТаблицаОтчета.ПоказатьУровеньГруппировокСтрок(1);
	
КонецПроцедуры

&НаКлиенте
Процедура Сформировать(Команда)
	
	Если ЗначениеЗаполнено(Отчет.Период) И ЗначениеЗаполнено(Отчет.ВидОтчета) Тогда
		СформироватьОтчетНаСервере();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ПриОткрытииНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПриОткрытииНаСервере()
	Отчет.ВидОтчета = "РСВ";
	ДатаПрошлогоПериода = ДобавитьМесяц(ТекущаяДатаСеанса(), -1);
	Отчет.Период.ДатаНачала = НачалоМесяца(ДатаПрошлогоПериода);
	Отчет.Период.ДатаОкончания = КонецМесяца(ДатаПрошлогоПериода);
КонецПроцедуры


