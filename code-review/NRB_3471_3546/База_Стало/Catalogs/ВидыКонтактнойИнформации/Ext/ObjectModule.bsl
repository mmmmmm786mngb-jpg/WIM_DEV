
#Если Клиент Тогда
	
Процедура ПриЗаписи(Отказ)

	Если (НЕ ОбменДанными.Загрузка) И (ОбщегоНазначения.ЗначениеНеЗаполнено(ВидОбъектаКонтактнойИнформации) ИЛИ ОбщегоНазначения.ЗначениеНеЗаполнено(Тип)) Тогда

		Предупреждение("Укажите вид и тип объекта контактной информации.");
		Отказ = Истина;

	КонецЕсли;

КонецПроцедуры

#КонецЕсли
