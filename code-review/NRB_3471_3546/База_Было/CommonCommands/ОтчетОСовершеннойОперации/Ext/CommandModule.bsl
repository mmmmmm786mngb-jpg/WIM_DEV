
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	#Если ТолстыйКлиентОбычноеПриложение Тогда

	ПараметрыОткрытия = Новый Структура("ОперацияЦБ,ДатаВыписки,Автоисполнение",ПараметрКоманды,ПараметрКоманды.Дата,Истина);

	ОткрытьФорму("Отчет.ОтчетОВыполненнойОперации_Новый.Форма.ФормаОтчета",ПараметрыОткрытия,ЭтотОбъект);

#КонецЕсли

		
КонецПроцедуры
