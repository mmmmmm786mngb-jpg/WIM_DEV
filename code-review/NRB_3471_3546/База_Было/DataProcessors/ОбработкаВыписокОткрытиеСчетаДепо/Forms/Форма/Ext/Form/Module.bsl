
&НаСервере
Процедура ОбработатьВыпискиНаСервере()
	РеквизитФормыВЗначение("Объект").ОбработатьВыписки();
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьВыписки(Команда)
	ОбработатьВыпискиНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПеренумероватьДокументыНаСервере()
	РеквизитФормыВЗначение("Объект").ПеренумероватьДокументы();
КонецПроцедуры

&НаКлиенте
Процедура ПеренумероватьДокументы(Команда)
	ПеренумероватьДокументыНаСервере();
КонецПроцедуры
