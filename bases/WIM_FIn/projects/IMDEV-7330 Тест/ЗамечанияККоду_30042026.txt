Доработки^

1) ПорогМассовогоПредрасчета = 5;  - ставим пока 1
	
2)	ТЗПары.Колонки.Добавить("УправляющаяКомпания",
		Новый ОписаниеТипов("СправочникСсылка.УправляющиеКомпании"));  
		- тип реквизита УправляющаяКомпания ->  Новый ОписаниеТипов("СправочникСсылка.Контрагенты")
		
3) NDFL_RDU_ИнициализироватьКэшОборотовПриНеобходимости  - надо вызывать в СоздатьДокументе,
а не в цикле каждой строки!

При этом надо учесть при выборе ПАР и эти условия:

		Если ОтборПоНомеруСтроки Тогда
			Если НомерСтрокиС <> 0 И СтрокаДокументов.НомерСтроки < НомерСтрокиС Тогда
				Продолжить;
			ИначеЕсли НомерСтрокиПо <> 0 И СтрокаДокументов.НомерСтроки > НомерСтрокиПо Тогда
				Прервать;
			КонецЕсли;
		КонецЕсли;
		       			
		Если НеСоздаватьДокументыПоКлиентамБезЦБ И КлиентыСЦБ.Найти(СтрокаДокументов.Клиент) = Неопределено Тогда
			Продолжить;
		КонецЕсли;
