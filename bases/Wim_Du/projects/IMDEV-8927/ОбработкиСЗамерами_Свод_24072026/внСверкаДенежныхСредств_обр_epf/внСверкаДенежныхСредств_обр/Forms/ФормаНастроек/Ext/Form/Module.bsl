

&НаКлиенте
Процедура Установить(Команда)
	
	Заголовок = "";
	ЗначенияПараметров = УстановитьПараметры(Заголовок);
	ОповеститьОВыборе(новый Структура("Значения,ЗаголовокНастроек", ЗначенияПараметров,Заголовок));
	
КонецПроцедуры

&НаСервере
Функция УстановитьПараметры(Заголовок)
	
	Значения = Новый Структура;
	значения.Вставить("НачДата",	Объект.НачДата);
	Значения.Вставить("КонДата", Объект.КонДата);
	
	Значения.Вставить("ПочтовыйЛогин", Объект.ПочтовыйЛогин);
	Значения.Вставить("СписокРассылки", XMLСтрока(Новый ХранилищеЗначения(Объект.СписокРассылки.Выгрузить())));
	
	Значения.Вставить("ДоговорИсключение", Объект.ДоговорИсключение);   //IMAPPS-20859
	Значения.Вставить("СчетИсключение", Объект.СчетИсключение);         //IMAPPS-20859
	
	ПредставлениеПочты = ?(Объект.СписокРассылки.Количество()>0, "Установлен список рассылки","");
	Заголовок = "Дата Т-1, " + ПредставлениеПочты;
	Заголовок = СокрЛП(Заголовок);

	Возврат Значения;
	
КонецФункции

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ТипЗнч(Параметры.НастройкиАлгоритма)=Тип("Структура") тогда
		
		Настройки = Параметры.НастройкиАлгоритма;
		ТекОбъект = РеквизитФормыВЗначение("Объект");
		ТекОбъект.УстановитьПользовательскиеНастройки(Настройки);
		ЗначениеВРеквизитФормы(ТекОбъект,"Объект");
				
	КонецЕсли;
	
	// IMAPPS-25493++
	////IMAPPS-20859 ++
	//Объект.ДоговорИсключение.Добавить(Справочники.ДоговорДУ.НайтиПоНаименованию("УК ВТБ УА"));
	//Объект.ДоговорИсключение.Добавить(Справочники.ДоговорДУ.НайтиПоНаименованию("УК ВТБ УИ"));
	//Объект.ДоговорИсключение.Добавить(Справочники.ДоговорДУ.НайтиПоНаименованию("УК ВТБ ПР"));
	//Объект.ДоговорИсключение.Добавить(Справочники.ДоговорДУ.НайтиПоНаименованию("ДУ 3485 (ВТБ ПФ СПН)"));
	//Объект.ДоговорИсключение.Добавить(Справочники.ДоговорДУ.НайтиПоНаименованию("СФР_ПР"));
	//
	//Объект.СчетИсключение.Добавить(Справочники.БанковскиеСчета.НайтиПоНаименованию("40701810600760000051"));
	////IMAPPS-20859 --
	// IMAPPS-25493--
	
КонецПроцедуры

&НаКлиенте
Процедура ДоговорИсключениеНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	Объект.ДоговорИсключение.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.ДоговорДУ");
КонецПроцедуры

&НаКлиенте
Процедура СчетИсключениеНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	Объект.СчетИсключение.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.БанковскиеСчета");
КонецПроцедуры
