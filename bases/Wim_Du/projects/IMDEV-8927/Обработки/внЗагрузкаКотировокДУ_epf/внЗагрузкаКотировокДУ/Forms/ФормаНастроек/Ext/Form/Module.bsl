
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ТипЗнч(Параметры.НастройкиАлгоритма)=Тип("Структура") Тогда
		
		Настройки = Параметры.НастройкиАлгоритма;
		ТекОбъект = РеквизитФормыВЗначение("Объект");
		ТекОбъект.УстановитьПользовательскиеНастройки(Настройки);
		ЗначениеВРеквизитФормы(ТекОбъект,"Объект");
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Установить(Команда)
	
	Заголовок = "";
	ЗначенияПараметров = УстановитьПараметры(Заголовок);
	ОповеститьОВыборе(новый Структура("Значения,ЗаголовокНастроек", ЗначенияПараметров,Заголовок));
	
КонецПроцедуры

&НаКлиенте
Процедура ПоУмолчанию(Команда)
	
	ПоУмолчаниюСервер();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыФункции

&НаСервере
Функция УстановитьПараметры(Заголовок)
	
	Значения = Новый Структура;
	Значения.Вставить("ОбработкаЗагрузкиАктивов", Объект.ОбработкаЗагрузкиАктивов);
	Значения.Вставить("КомандаЗагрузкиАктивов", Объект.КомандаЗагрузкиАктивов);
	Значения.Вставить("ТестовыйРежим", Объект.ТестовыйРежим);
	Значения.Вставить("ПочтовыйЛогин", Объект.ПочтовыйЛогин);
	Значения.Вставить("MOEX_BMBRG", Объект.MOEX_BMBRG);
	Значения.Вставить("INTERFAX", Объект.INTERFAX);	
	Значения.Вставить("СписокРассылки", XMLСтрока(Новый ХранилищеЗначения(Объект.СписокРассылки.Выгрузить())));
	
	ПредставлениеПочты = ?(Объект.СписокРассылки.Количество()>0, ", Установлен список рассылки","");
	Заголовок = "Загрузка активов:"+ Объект.ОбработкаЗагрузкиАктивов + " ("+ Объект.КомандаЗагрузкиАктивов+")"+ ?(Объект.ТестовыйРежим,"Тестовый режим", "")+ ПредставлениеПочты + ", ";
	
	Возврат Значения;
	
КонецФункции

&НаСервере
Процедура ПоУмолчаниюСервер()
	
	Объект.MOEX_BMBRG = Истина;
	Объект.INTERFAX = Истина;
	Объект.ПочтовыйЛогин = Справочники.УчетныеЗаписиЭлектроннойПочты.СистемнаяУчетнаяЗаписьЭлектроннойПочты;
	
	Объект.СписокРассылки.Очистить();
	
	СтрокаТЧ = Объект.СписокРассылки.Добавить();
	СтрокаТЧ.ПочтовыйЯщик = "Kirill.Ikonnikov@vtbcapital.com";
	СтрокаТЧ.Представление = "Kirill.Ikonnikov@vtbcapital.com";
	
	СтрокаТЧ = Объект.СписокРассылки.Добавить();
	СтрокаТЧ.ПочтовыйЯщик = "VTBAMBackoffice_DU@vtbcapital.com";
	СтрокаТЧ.Представление = "VTBAMBackoffice_DU@vtbcapital.com";
	
	СтрокаТЧ = Объект.СписокРассылки.Добавить();
	СтрокаТЧ.ПочтовыйЯщик = "IMAppSupport@vtbcapital.com";
	СтрокаТЧ.Представление = "IMAppSupport@vtbcapital.com";
	
	СтрокаТЧ = Объект.СписокРассылки.Добавить();
	СтрокаТЧ.ПочтовыйЯщик = "Yuri.Ivanov@vtbcapital.com";
	СтрокаТЧ.Представление = "Yuri.Ivanov@vtbcapital.com";
	
КонецПроцедуры

#КонецОбласти