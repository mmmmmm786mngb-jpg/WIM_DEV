# ОПТИМИЗАЦИЯ IMDEV-9336

Рабочие копии внешних обработок для доработки замеров производительности.
Исходники скопированы из `ОРИГИНАЛЫ/` (не изменять оригиналы).

## Объекты

| Обработка | Задача | Что доработать |
|-----------|--------|----------------|
| `внСверкаЦБ_epf` | IMDEV-9336 п.2 | Заменить `ВесЗамераСверки = 1` на `ТЗ.Количество()` в `ВыполнитьСверку()` |
| `внОтрицательныеОстаткиНаАналитическомСчете_epf` | IMDEV-8274 / 9336 п.3 | Добавить `ОценкаПроизводительности`, команду `ФоновоеВыполнение`, вес = строки ОСВ с минусом |

## Сборка EPF

Собранные файлы: `Обработки/внСверкаЦБ.epf` (v1.07), `Обработки/внОтрицательныеОстаткиНаАналитическомСчете.epf` (v1.2).

Пересборка через базу WIM_DU без авторизации: `Srvr="localhost";Ref="WIM_DU";` (skill `epf-build`).

## Документация

Подробности по замерам и весу: `Документация/jira_comment_zamery_9336.txt` (раздел «Предложения по улучшению замеров»).
