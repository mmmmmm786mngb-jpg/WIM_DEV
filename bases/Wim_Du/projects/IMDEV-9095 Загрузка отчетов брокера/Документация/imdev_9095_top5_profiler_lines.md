# IMDEV-9095: TOP-5 строк профилировщика

**Источник:** `Тестирование/Замеры_01_07_2026.pff`  
**Сессия:** «Создать всё» (хост SMSK02MG138U, база `AVC_REGR_CLEAN_DU`)  
**Объем:** 801 проведенный документ `ОперацияПоСчетуБрокера`  
**Дата:** 01.07.2026  
**Связанный отчет:** [imdev_9095_optimization_analysis.html](imdev_9095_optimization_analysis.html)

---

## Сводка

| # | Модуль | Строка | Время | Вызовов | мс/вызов | Зона |
|---|--------|--------|-------|---------|----------|------|
| 1 | EPF ObjectModule | 258 | **500,2 с** | 754 | 663 | EPF |
| 2 | Документ.ОперацияПоСчетуБрокера | 1614 | **209,2 с** | 801 | 261 | Конфигурация |
| 3 | ОбщийМодуль.ОбщегоНазначенияДУ | 2447 | **171,8 с** | 793 | 217 | Конфигурация |
| 4 | Документ.ОперацияПоСчетуБрокера | 1610 | **117,1 с** | 277 | 423 | Конфигурация |
| 5 | Документ.ОперацияПоСчетуБрокера | 1071 | **113,8 с** | 277 | 411 | Конфигурация |

**Сумма TOP-5:** ~1 112 с (~51% от общего времени сессии 2 165 с).

---

## 1. EPF ObjectModule, строка 258 — 500,2 с

**Процедура:** `СоздатьОперацииПоНКД()`  
**Вызовов:** 754 (по одному на документ НКД)

```bsl
ОбОперация.Записать(РежимЗаписиДокумента.Проведение);
```

**Что происходит:** синхронное проведение каждого документа в цикле по ТЧ «Купон».  
**Доля в процедуре НКД:** ~99% (503 с из 503 с).  
**Рекомендация 9095:** пакетная запись / фоновое проведение / длительная операция БСП.

---

## 2. Документ.ОперацияПоСчетуБрокера, строка 1614 — 209,2 с

**Вызовов:** 801 (на каждое проведение)

```bsl
ОбщегоНазначенияДУ.ФормированиеДокументаОтражениеОперацийВРегистрахВУ(Ссылка);
```

**Что происходит:** отражение операции в регистрах управленческого учета при проведении.  
**Чистое время строки:** 0,37 с (остальное — вложенные вызовы).  
**Рекомендация:** снижается при уменьшении числа синхронных проведений; отдельная оптимизация — вне EPF.

---

## 3. ОбщийМодуль.ОбщегоНазначенияДУ, строка 2447 — 171,8 с

**Вызовов:** 793

```bsl
Результат = Запрос.Выполнить();
```

**Что происходит:** тяжелый запрос внутри цепочки формирования отражения в регистрах ВУ (вызывается из п.2).  
**Чистое время:** 171,8 с (запрос — узкое место внутри конфигурации).  
**Рекомендация:** анализ плана запроса / кэширование на уровне конфигурации; для 9095 — косвенный эффект от пакетного проведения.

---

## 4. Документ.ОперацияПоСчетуБрокера, строка 1610 — 117,1 с

**Вызовов:** 277 (не на все 801 документ — только при необходимости переоценки)

```bsl
СформироватьПереоценку(Отказ);
```

**Что происходит:** блок переоценки валютных остатков при проведении.  
**Рекомендация:** оценить бизнес-необходимость переоценки при массовой автозагрузке.

---

## 5. Документ.ОперацияПоСчетуБрокера, строка 1071 — 113,8 с

**Вызовов:** 277

```bsl
ТаблицаПереоценка = УчетДоходовРасходовСервер.ПодготовитьТаблицуПереоценкаВалютныхОстатковПоПроводкамДокумента(
    РеквизитыПереоценка, Движения, Отказ);
```

**Что происходит:** подготовка таблицы переоценки в `УчетДоходовРасходовСервер` (продолжение п.4).  
**Модуль:** `ОбщийМодуль.УчетДоходовРасходовСервер` (вложенный вызов).

---

## Вывод

1. **Единственная строка EPF в TOP-5** — `Записать(Проведение)` (п.1). Это точка входа оптимизации 9095.
2. **Пп. 2-5** — стоимость проведения в типовой логике ДУ; убираются в основном сокращением числа синхронных проведений.
3. Строки EPF с `НайтиПоНаименованию` (1,5 с) и `ПолучитьКурсВалюты` (0,6 с) **не входят в TOP-5**.

**Машиночитаемый список:** `Тестирование/reports/top5_profiler_lines.json`  
**Скрипт извлечения:** `Скрипты/top5_pff_lines.py`
