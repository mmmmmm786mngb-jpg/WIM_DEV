--- ALEKSEY/ObjectModule.bsl
+++ FEDOR/ObjectModule.bsl
@@ -17,4 +17,11 @@
 //IMAPPS-19973--
 
+// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+Перем ВесКоличествоМандатов;
+// - FVasiliev IMDEV-8972 24.06.2026
+
+// + FVasiliev IMDEV-8829 03.06.2026
+Перем РежимОтладкиСервиса Экспорт; // Истина — отладочный режим (пропускать вызовы)
+// - FVasiliev IMDEV-8829 03.06.2026
 
 #Область ПроцедурыФункцииДополнительныхОтчетовОбработок
@@ -29,5 +36,5 @@
 	ПараметрыРегистрации.Вставить("Назначение",МассивСтрокИмен);
 	ПараметрыРегистрации.Вставить("Наименование", "Выгрузка остатков МО ФО");
-	ПараметрыРегистрации.Вставить("Версия", "1.35");
+	ПараметрыРегистрации.Вставить("Версия", "1.38");
 	ПараметрыРегистрации.Вставить("БезопасныйРежим",Ложь);
 	ПараметрыРегистрации.Вставить("Информация","Выгрузка остатков МО ФО");
@@ -50,5 +57,5 @@
 	
 	// IMDEV-8829 ++ команда-координатор параллельной выгрузки пачками мандатов
-	ДобавитьКоманду(ТаблицаКоманд,"Выгрузка остатков МО ФО (пачками, в фоне)", "ФоноваяВыгрузкаПачками", "ВызовСерверногоМетода");
+	ДобавитьКоманду(ТаблицаКоманд,"Выгрузка остатков МО ФО (не использовать!)", "ФоноваяВыгрузкаПачками", "ВызовСерверногоМетода");
 	// IMDEV-8829 --
 	
@@ -85,26 +92,78 @@
 Процедура ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполненияКоманды) Экспорт
 	
+	// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	Узел = "";
+	// - FVasiliev IMDEV-8972 09.06.2026
+		
 	// IMDEV-8829 ++ координатор параллельной выгрузки пачками мандатов
-	Если ИдентификаторКоманды = "ФоноваяВыгрузкаПачками" Тогда
-		УстановитьНастройки(ПараметрыВыполненияКоманды);
-		ФоноваяВыгрузкаПачками(ПараметрыВыполненияКоманды);
-		Возврат;
-	КонецЕсли;
-	// IMDEV-8829 --
-	
-	Если ИдентификаторКоманды <> "ФоноваяВыгрузка" Тогда Возврат; КонецЕсли;
+	УстановитьНастройки(ПараметрыВыполненияКоманды);
 	
 	Если ТипЗнч(ПараметрыВыполненияКоманды) = Тип("Структура") тогда
 		Если ПараметрыВыполненияКоманды.Свойство("Узел") Тогда
 			ПараметрыВыполненияКоманды.Вставить("Узел", ПараметрыВыполненияКоманды.Узел);
+			// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+			Узел = ПараметрыВыполненияКоманды.Узел;
+			// - FVasiliev IMDEV-8972 09.06.2026
 		КонецЕсли;
 	КонецЕсли;
 	
+	//Если ИдентификаторКоманды = "ФоноваяВыгрузкаПачками" Тогда
+	Если ИдентификаторКоманды = "ФоноваяВыгрузка" И КоличествоПотоков > 1 Тогда
+		
+		//УстановитьНастройки(ПараметрыВыполненияКоманды);
+		ФоноваяВыгрузкаПачками(ПараметрыВыполненияКоманды);
+		
+		// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+		Вес = ВесКоличествоМандатов;
+		ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ВыполнитьКоманду_" + Узел + "_" + КоличествоПотоков + "_потоков_" + "_Сервер", ВремяНачала, Вес);
+		// - FVasiliev IMDEV-8972 09.06.2026
+		Возврат;
+	КонецЕсли;
+	// IMDEV-8829 --
+	//Если ИдентификаторКоманды <> "ФоноваяВыгрузка" Тогда Возврат; КонецЕсли;
+	
 	ДатаПоз = ТекущаяДата();
 	
-	ПредставлениеНастроек = УстановитьНастройки(ПараметрыВыполненияКоманды);
+	//ПредставлениеНастроек = УстановитьНастройки(ПараметрыВыполненияКоманды);
 	
 	ОписаниеОбменов.ВыполнитьКомандуПоОбменам(ЭтотОбъект, ИдентификаторКоманды, ПараметрыВыполненияКоманды, ПараметрыВыполненияКоманды.ДополнительнаяОбработкаСсылка.БезопасныйРежим);
 	
+	// + FVasiliev IMDEV-8829 15.07.2026
+	Если ЗначениеЗаполнено(ИдентификаторСеансаОбмена)
+		И ПараметрыВыполненияКоманды.Свойство("ПараметрыДополнительнойОбработки") Тогда
+		ПараметрыПачки = ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки;
+		Если ПараметрыПачки.Свойство("АдресРезультата") Тогда
+			ПоместитьВоВременноеХранилище(ДанныеДляРассылки, ПараметрыПачки.АдресРезультата);
+		КонецЕсли;
+	КонецЕсли;
+	// - FVasiliev IMDEV-8829 15.07.2026
+	
+	// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+	Вес = ВесКоличествоМандатов;
+	НомерПачкиТек = "";
+	Если ПараметрыВыполненияКоманды.Свойство("ПараметрыДополнительнойОбработки")
+		И ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки.Свойство("НомерПачки") Тогда
+		НомерПачкиТек = "пачка_" + ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки.НомерПачки + "_";
+	КонецЕсли;
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ВыполнитьКоманду_" + Узел + "_" + НомерПачкиТек + "/" + КоличествоПотоков + "_поток_" + "_Сервер", ВремяНачала, Вес);
+	// - FVasiliev IMDEV-8972 09.06.2026
+
+	// + FVasiliev IMDEV-8829: сохранение лога воркера
+	Если вклОтладка
+		И ПараметрыВыполненияКоманды.Свойство("ПараметрыДополнительнойОбработки")
+		И ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки.Свойство("КаталогЛогов") Тогда
+		КаталогЛогов = ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки.КаталогЛогов;
+		НомерПачки = ПараметрыВыполненияКоманды.ПараметрыДополнительнойОбработки.НомерПачки;
+		Попытка
+			ТекстовыйЛог.Записать(КаталогЛогов + "\пачка_" + НомерПачки + ".txt");
+		Исключение
+			ЗаписьЖурналаРегистрации("IMDEV-8829.Выгрузка остатков МО-ФО", УровеньЖурналаРегистрации.Ошибка, ,
+				"Ошибка сохранения лога пачки " + НомерПачки + ": " + ОписаниеОшибки());
+			ТекстовыйЛог.ДобавитьСтроку("IMDEV-8829: ошибка сохранения лога: " + ОписаниеОшибки());
+		КонецПопытки;
+	КонецЕсли;
+	// - FVasiliev IMDEV-8829
+
 КонецПроцедуры
 
@@ -136,7 +195,8 @@
 	Объект.ИсполнениеПоСуммеМО = Ложь;
 	Объект.КоличествоПортфелейВПакете = 50;
-	// IMDEV-8829 ++ число параллельных пачек по умолчанию
-	Объект.КоличествоПачекМандатов = 3;
-	// IMDEV-8829 --
+	// + FVasiliev IMDEV-8829 03.06.2026
+	Объект.КоличествоПотоков = 1;
+	Объект.КоличествоПотоков = 1;
+	// - FVasiliev IMDEV-8829 03.06.2026
 КонецПроцедуры
 
@@ -153,8 +213,8 @@
 	Значения.Вставить("ДатаНачалоВыгрузкиРЕПО", ЭтотОбъект.ДатаНачалоВыгрузкиРЕПО);
 	Значения.Вставить("КоличествоПортфелейВПакете", ЭтотОбъект.КоличествоПортфелейВПакете);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	Значения.Вставить("КоличествоПотоков", ЭтотОбъект.КоличествоПотоков);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Значения.Вставить("МандатДляТеста", ЭтотОбъект.МандатДляТеста);
-	// IMDEV-8829 ++ число параллельных пачек мандатов
-	Значения.Вставить("КоличествоПачекМандатов", ЭтотОбъект.КоличествоПачекМандатов);
-	// IMDEV-8829 --
 	
 	//IMAPPS-19973++
@@ -206,5 +266,5 @@
 		
 		// IMDEV-8829 ++ параметры пачечного режима
-		Настройки.Значения.Свойство("КоличествоПачекМандатов", КоличествоПачекМандатов);
+		Настройки.Значения.Свойство("КоличествоПотоков", КоличествоПотоков);
 		Настройки.Значения.Свойство("ИдентификаторСеансаОбмена", ИдентификаторСеансаОбмена);
 		Если Настройки.Значения.Свойство("МандатыПачки")
@@ -330,5 +390,10 @@
 		КонецЕсли;
 		
-		РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		// + FVasiliev IMDEV-8829 15.07.2026
+		// РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		Если НЕ ЗначениеЗаполнено(ИдентификаторСеансаОбмена) Тогда
+			РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		КонецЕсли;
+		// - FVasiliev IMDEV-8829 15.07.2026
 		
 	Исключение
@@ -357,100 +422,8 @@
 
 // IMDEV-8829 ++
-// Описание: Записывает в журнал регистрации все параметры вызова фонового задания воркера.
-//
-// Параметры:
-//  ПараметрыМетода - Массив - аргументы для ДополнительныеОтчетыИОбработки.ВыполнитьКоманду.
-//  НомерПачки      - Число  - индекс пачки (0-based).
-//  СессияОбмена    - Строка - общий идентификатор сеанса обмена.
-//
-Процедура ЗаписатьОтладкуПараметровФоновогоЗадания(ПараметрыМетода, НомерПачки, СессияОбмена)
-	
-	ТекстКомментария = "IMDEV-8829 отладка пачка " + (НомерПачки + 1)
-		+ ", сеанс " + СессияОбмена + Символы.ПС;
-	
-	Для ИндексПараметра = 0 По ПараметрыМетода.ВГраница() Цикл
-		
-		ИмяПараметра = "ПараметрыМетода[" + ИндексПараметра + "]";
-		ЗначениеПараметра = ПараметрыМетода[ИндексПараметра];
-		
-		Если ТипЗнч(ЗначениеПараметра) = Тип("Структура") Тогда
-			
-			Для Каждого КлючИЗначение Из ЗначениеПараметра Цикл
-				ИмяВложенного = ИмяПараметра + "." + КлючИЗначение.Ключ;
-				ТекстКомментария = ТекстКомментария + ИмяВложенного + " = "
-					+ ПредставлениеЗначенияДляОтладкиВыгрузки(КлючИЗначение.Значение, ИмяВложенного) + Символы.ПС;
-			КонецЦикла;
-			
-		Иначе
-			
-			ТекстКомментария = ТекстКомментария + ИмяПараметра + " = "
-				+ ПредставлениеЗначенияДляОтладкиВыгрузки(ЗначениеПараметра, ИмяПараметра) + Символы.ПС;
-			
-		КонецЕсли;
-		
-	КонецЦикла;
-	
-	ЗаписьЖурналаРегистрации("Отладка", УровеньЖурналаРегистрации.Информация,,, ТекстКомментария);
-	
-КонецПроцедуры
-
-// Описание: Формирует строковое представление значения для отладочного лога координатора.
-//
-// Параметры:
-//  Значение       - Произвольный - значение параметра.
-//  ИмяПараметра   - Строка       - имя параметра (для особой обработки МандатыПачки).
-//
-// Возвращаемое значение:
-//  Строка - представление для журнала регистрации.
-//
-Функция ПредставлениеЗначенияДляОтладкиВыгрузки(Значение, ИмяПараметра = "")
-	
-	Если Значение = Неопределено Тогда
-		Возврат "<Неопределено>";
-	КонецЕсли;
-	
-	ТипЗначения = ТипЗнч(Значение);
-	
-	Если ТипЗначения = Тип("Структура") Тогда
-		
-		СтрокиПредставления = Новый Массив;
-		Для Каждого КлючИЗначение Из Значение Цикл
-			СтрокиПредставления.Добавить(КлючИЗначение.Ключ + "="
-				+ ПредставлениеЗначенияДляОтладкиВыгрузки(КлючИЗначение.Значение, КлючИЗначение.Ключ));
-		КонецЦикла;
-		Возврат "{" + СтрСоединить(СтрокиПредставления, "; ") + "}";
-		
-	ИначеЕсли ТипЗначения = Тип("Массив") Тогда
-		
-		Если ИмяПараметра = "МандатыПачки"
-			Или Прав(ИмяПараметра, 13) = "МандатыПачки" Тогда
-			
-			СтрокиМандатов = Новый Массив;
-			Для Каждого ЭлементМандата Из Значение Цикл
-				СтрокиМандатов.Добавить(Строка(ЭлементМандата));
-			КонецЦикла;
-			Возврат "Количество=" + Значение.Количество() + "; " + СтрСоединить(СтрокиМандатов, ", ");
-			
-		КонецЕсли;
-		
-		СтрокиЭлементов = Новый Массив;
-		Для Каждого ЭлементМассива Из Значение Цикл
-			СтрокиЭлементов.Добавить(ПредставлениеЗначенияДляОтладкиВыгрузки(ЭлементМассива));
-		КонецЦикла;
-		Возврат "[" + СтрСоединить(СтрокиЭлементов, ", ") + "]";
-		
-	Иначе
-		
-		Возврат Строка(Значение);
-		
-	КонецЕсли;
-	
-КонецФункции
-
 // Описание: Координатор параллельной выгрузки остатков МО -> ФО пачками мандатов.
 //   Делит все мандаты с остатками на N непересекающихся пачек, запускает по одному
 //   фоновому заданию на пачку (штатная команда "ФоноваяВыгрузка"), дожидается завершения
 //   всех заданий (барьер) и один раз выполняет финальную рассылку по перерасчёту РСА.
-//   В отладочном логе (событие «Отладка») перед запуском каждого воркера пишутся его параметры.
 //
 // Параметры:
@@ -463,5 +436,5 @@
 	СессияОбмена = Строка(Новый УникальныйИдентификатор);
 	
-	КолвоПачек = ?(ЗначениеЗаполнено(КоличествоПачекМандатов), КоличествоПачекМандатов, 3);
+	КолвоПачек = ?(ЗначениеЗаполнено(КоличествоПотоков), КоличествоПотоков, 3);
 	
 	МассивМандатов = ПолучитьМассивВсехМандатовДляВыгрузки();
@@ -479,5 +452,23 @@
 	КонецЕсли;
 	
+	// + FVasiliev IMDEV-8829: каталог для логов воркеров
+	КаталогЛогов = "";
+	Если вклОтладка Тогда
+		Попытка
+			КаталогЛогов = КаталогВременныхФайлов() + "\логи_МО_ФО_" + СессияОбмена;
+			СоздатьКаталог(КаталогЛогов);
+		Исключение
+			КаталогЛогов = "";
+			ЗаписьЖурналаРегистрации("IMDEV-8829.Выгрузка остатков МО-ФО", УровеньЖурналаРегистрации.Ошибка, ,
+				"Не удалось создать каталог логов: " + ОписаниеОшибки());
+			ТекстовыйЛог.ДобавитьСтроку("IMDEV-8829: не удалось создать каталог логов: " + ОписаниеОшибки());
+		КонецПопытки;
+	КонецЕсли;
+	// - FVasiliev IMDEV-8829
+
 	МассивЗаданий = Новый Массив;
+	// + FVasiliev IMDEV-8829 15.07.2026
+	МассивАдресов = Новый Массив;
+	// - FVasiliev IMDEV-8829 15.07.2026
 	Для НомерПачки = 0 По Пачки.ВГраница() Цикл
 		
@@ -493,8 +484,19 @@
 		НастройкиПачки.Значения.Вставить("МандатыПачки", Пачки[НомерПачки]);
 		НастройкиПачки.Значения.Вставить("ИдентификаторСеансаОбмена", СессияОбмена);
+		НастройкиПачки.Вставить("НомерПачки", НомерПачки + 1);
+		Если КаталогЛогов <> "" Тогда
+			НастройкиПачки.Вставить("КаталогЛогов", КаталогЛогов);
+		КонецЕсли;
+		
+		// + FVasiliev IMDEV-8829 15.07.2026
+		Адрес = ПоместитьВоВременноеХранилище(Новый Структура, Новый УникальныйИдентификатор);
+		НастройкиПачки.Вставить("АдресРезультата", Адрес);
+		МассивАдресов.Добавить(Адрес);
+		// - FVasiliev IMDEV-8829 15.07.2026
 		
 		ПараметрыКоманды = Новый Структура;
 		ПараметрыКоманды.Вставить("ДополнительнаяОбработкаСсылка", БазоваяОбработкаСсылка);
-		ПараметрыКоманды.Вставить("ИдентификаторКоманды", "ФоноваяВыгрузка");
+		//ПараметрыКоманды.Вставить("ИдентификаторКоманды", "ФоноваяВыгрузка");
+		ПараметрыКоманды.Вставить("ИдентификаторКоманды", "ФоноваяВыгрузкаПачками");
 		ПараметрыКоманды.Вставить("ПараметрыДополнительнойОбработки", НастройкиПачки);
 		Если ПараметрыВыполненияКоманды.Свойство("Узел") Тогда
@@ -509,8 +511,4 @@
 		ПараметрыМетода.Добавить(Неопределено);
 		
-		// IMDEV-8829 отладка: лог параметров воркера перед запуском фонового задания
-		ЗаписатьОтладкуПараметровФоновогоЗадания(ПараметрыМетода, НомерПачки, СессияОбмена);
-		
-		// IMDEV-8829: на платформе Wim_Mo используется ФоновыеЗадания.Выполнить (не ВыполнитьЗадание)
 		Задание = ФоновыеЗадания.Выполнить(
 			"ДополнительныеОтчетыИОбработки.ВыполнитьКоманду",
@@ -522,22 +520,65 @@
 	КонецЦикла;
 	
-	// Барьер: дождаться завершения всех воркеров
-	Попытка
-		ФоновыеЗадания.ОжидатьЗавершения(МассивЗаданий);
-	Исключение
-		Для Каждого Задание Из МассивЗаданий Цикл
-			Если Задание.Состояние = СостояниеФоновогоЗадания.ЗавершеноАварийно Тогда
-				ТекстОшибки = "IMDEV-8829: ошибка фонового задания пачки: ";
-				Если Задание.ИнформацияОбОшибке <> Неопределено Тогда
-					ТекстОшибки = ТекстОшибки + КраткоеПредставлениеОшибки(Задание.ИнформацияОбОшибке);
-				КонецЕсли;
-				ТекстовыйЛог.ДобавитьСтроку(ТекстОшибки);
-			КонецЕсли;
+	// + FVasiliev IMDEV-8829 15.07.2026
+	// Барьер: дождаться завершения всех воркеров поочерёдно
+	Для Инд = 0 По МассивЗаданий.ВГраница() Цикл
+		МассивЗаданий[Инд] = МассивЗаданий[Инд].ОжидатьЗавершенияВыполнения();
+	КонецЦикла;
+	
+	// Проверка ошибок после барьера
+	ЕстьОшибка = Ложь;
+	Для Каждого Задание Из МассивЗаданий Цикл
+		Если Задание.Состояние = СостояниеФоновогоЗадания.ЗавершеноАварийно Тогда
+			ЕстьОшибка = Истина;
+			ТекстОшибки = "IMDEV-8829: ошибка фонового задания '" + Задание.Наименование + "'";
+			ТекстовыйЛог.ДобавитьСтроку(ТекстОшибки);
+		КонецЕсли;
+	КонецЦикла;
+	// - FVasiliev IMDEV-8829 15.07.2026
+	
+	// + FVasiliev IMDEV-8829: сбор логов воркеров
+	Если КаталогЛогов <> "" Тогда
+		Для НомерПачки = 1 По КолвоПачек Цикл
+			ИмяФайла = КаталогЛогов + "\пачка_" + НомерПачки + ".txt";
+			Попытка
+				ЛогПачки = Новый ТекстовыйДокумент;
+				ЛогПачки.Прочитать(ИмяФайла);
+				ТекстовыйЛог.ДобавитьСтроку("=== Пачка " + НомерПачки + " ===");
+				ТекстовыйЛог.ДобавитьСтроку(ЛогПачки.ПолучитьТекст());
+			Исключение
+				ЗаписьЖурналаРегистрации("IMDEV-8829.Выгрузка остатков МО-ФО", УровеньЖурналаРегистрации.Ошибка, ,
+					"Ошибка чтения лога пачки " + НомерПачки + ": " + ОписаниеОшибки());
+				ТекстовыйЛог.ДобавитьСтроку("IMDEV-8829: ошибка чтения лога пачки " + НомерПачки + ": " + ОписаниеОшибки());
+			КонецПопытки;
 		КонецЦикла;
-		ВызватьИсключение;
-	КонецПопытки;
+		УдалитьФайлы(КаталогЛогов, "");
+	КонецЕсли;
+	// - FVasiliev IMDEV-8829
 	
 	// Единая финальная рассылка по перерасчёту РСА по общему сеансу
 	ВыполнитьФинальнуюРассылкуРСА(СессияОбмена, НачалоОбработки, ТекущаяДатаСеанса());
+	
+	// + FVasiliev IMDEV-8829 15.07.2026
+	Если УчетнаяЗаписьПочты.Пустая() Или СписокРассылки.Количество() = 0 Тогда
+		Возврат;
+	КонецЕсли;
+	
+	ЕстьОшибка = Ложь;
+	Для Каждого Адрес Из МассивАдресов Цикл
+		ДанныеПачки = ПолучитьИзВременногоХранилища(Адрес);
+		Если ТипЗнч(ДанныеПачки) = Тип("Структура")
+			И ДанныеПачки.Свойство("КоличествоПортфелей") Тогда
+			ДанныеДляРассылки.КоличествоПортфелей = ДанныеДляРассылки.КоличествоПортфелей + ДанныеПачки.КоличествоПортфелей;
+			ДанныеДляРассылки.КоличествоФактическихПозиций = ДанныеДляРассылки.КоличествоФактическихПозиций + ДанныеПачки.КоличествоФактическихПозиций;
+			ДанныеДляРассылки.КоличествоОшибок = ДанныеДляРассылки.КоличествоОшибок + ДанныеПачки.КоличествоОшибок;
+			Для Каждого СтрокаОшибки Из ДанныеПачки.ПозицииСОшибкой Цикл
+				ЗаполнитьЗначенияСвойств(ДанныеДляРассылки.ПозицииСОшибкой.Добавить(), СтрокаОшибки);
+			КонецЦикла;
+		КонецЕсли;
+	КонецЦикла;
+	
+	ТекстОповещения = ?(ЕстьОшибка, "Одна или несколько пачек завершились аварийно. Подробности в логе.", "");
+	РассылкаСообщения("Выгрузка остатков МО -> ФО (пачки)", ТекстОповещения, (ЕстьОшибка Или ДанныеДляРассылки.КоличествоОшибок > 0), Ложь);
+	// - FVasiliev IMDEV-8829 15.07.2026
 	
 КонецПроцедуры
@@ -553,4 +594,7 @@
 	Отбор = СформироватьОтборДляФоновогоЗадания();
 	ТаблицаМандатов = ПолучитьМассивМандатовДляОтбора(Отбор);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	ВесКоличествоМандатов = ТаблицаМандатов.Количество();
+	// - FVasiliev IMDEV-8829 03.06.2026
 	ТаблицаМандатов.Свернуть("Мандат");
 	Возврат ТаблицаМандатов.ВыгрузитьКолонку("Мандат");
@@ -629,4 +673,9 @@
 //
 Процедура Заполнить(Отбор, ДатаЗаполнения) Экспорт
+	
+	// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 09.06.2026
+	
 	// IMDEV-8829 ++ в пачечном режиме список мандатов задан координатором, не перезаполняем
 	Если ОтборМандат И НЕ ЗначениеЗаполнено(ИдентификаторСеансаОбмена) Тогда
@@ -670,4 +719,11 @@
 		
 	КонецЦикла;
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	//Вес = РЕПОДеньги.Количество() + ЦенныеБумаги.Количество() + ДенежныеСредства.Количество() 
+	//+ СЧА_РСА.Количество() + Блокировки.Количество() + ИсполненияПоСделкам.Количество();
+	ВесКоличествоМандатов = Отбор.ОтборМандаты.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_Заполнить_Сервер", ВремяНачала, ВесКоличествоМандатов);
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 КонецПроцедуры // Заполнить()
@@ -902,4 +958,8 @@
 Процедура Опубликовать(ПересчитатьРыночнуюСтоимость = Ложь, ПересчитатьКомплаинс = Ложь, ДатаПубликации = Неопределено) Экспорт
 	
+	// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 09.06.2026
+	
 	Если ДатаПубликации=Неопределено Тогда
 		ДатаПубликации = ТекущаяДатаСеанса();
@@ -1054,7 +1114,13 @@
 				СформироватьПакетФлагПозицияНеВыгружена(СоответствиеПортфелей, ФабрикаСервиса, Прокси, ПакетХДТОФлагПозицииНеВыгружена);
 				
-				Ответ = Прокси.DownloadPosition(ПакетХДТОФлагПозицииНеВыгружена);
+				// + FVasiliev IMDEV-8829 03.06.2026
+				//Ответ = Прокси.DownloadPosition(ПакетХДТОФлагПозицииНеВыгружена);
+				Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТОФлагПозицииНеВыгружена);
+				// - FVasiliev IMDEV-8829 03.06.2026
 				
-				Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+				// + FVasiliev IMDEV-8829 03.06.2026
+				//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+				Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+				// - FVasiliev IMDEV-8829 03.06.2026
 				Если ЗначениеЗаполнено(Рез.ТекстОшибки) Тогда
 					ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОшибки);
@@ -1068,9 +1134,15 @@
 				КонецЕсли;
 				
-				Ответ = Прокси.DownloadPosition(ПакетХДТО);
+				// + FVasiliev IMDEV-8829 03.06.2026
+			//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+			Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+			// - FVasiliev IMDEV-8829 03.06.2026
 				
 				ЕстьОшибкиПортфель = Ложь;
 				
-				Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+				// + FVasiliev IMDEV-8829 03.06.2026
+				//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+				Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+				// - FVasiliev IMDEV-8829 03.06.2026
 				Если Рез.ЕстьОшибки Тогда
 					ЕстьОшибкиПортфель = Истина;
@@ -1125,7 +1197,13 @@
 				КонецЕсли;
 				
-				Ответ = Прокси.DownloadOrders(ПакетБлокировкиХДТО);
-				
-				Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+			// + FVasiliev IMDEV-8829 03.06.2026
+			//Ответ = Прокси.DownloadOrders(ПакетБлокировкиХДТО);
+			Ответ = ВызватьDownloadOrders(Прокси, ПакетБлокировкиХДТО);
+			// - FVasiliev IMDEV-8829 03.06.2026
+			
+			// + FVasiliev IMDEV-8829 03.06.2026
+			//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+			Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, Неопределено, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+			// - FVasiliev IMDEV-8829 03.06.2026
 				Если Рез.ЕстьОшибки Тогда
 					ЕстьОшибки = Истина;
@@ -1172,7 +1250,13 @@
 				СформироватьПакетФлагПозицияВыгруженаСОшибкой(СоответствиеПортфелей, ФабрикаСервиса, Прокси, ПакетХДТОФлагПозиции);
 				
-				Ответ = Прокси.DownloadPosition(ПакетХДТОФлагПозиции);
+				// + FVasiliev IMDEV-8829 03.06.2026
+				//Ответ = Прокси.DownloadPosition(ПакетХДТОФлагПозиции);
+				Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТОФлагПозиции);
+				// - FVasiliev IMDEV-8829 03.06.2026
 				
-				Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+				// + FVasiliev IMDEV-8829 03.06.2026
+				//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+				Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+				// - FVasiliev IMDEV-8829 03.06.2026
 				Если ЗначениеЗаполнено(Рез.ТекстОшибки) Тогда
 					ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОшибки);
@@ -1229,4 +1313,9 @@
 	//МассивФайлов.Добавить(Новый Структура("ИмяФайла, АдресФайлаВоВременномХранилище", "ЗапросУстановки.xml", АдресФайлаЗапроса)); 
 	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	Вес = КоличествоПортфелейВсего;
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_Опубликовать_Сервер", ВремяНачала, Вес);
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 КонецПроцедуры // Опубликовать()
 
@@ -1343,9 +1432,15 @@
 	КонецЦикла;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	
 	ЕстьОшибкиПортфель = Ложь;
 	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелОбменаДляРегистрацииНенайденных);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если Рез.ЕстьОшибки Тогда
 		ЕстьОшибкиПортфель = Истина;
@@ -1365,4 +1460,8 @@
 
 Процедура ОпубликоватьИсполнениеСделок(ФабрикаСервиса, Прокси)
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
 
 	Запрос = новый Запрос;
@@ -1379,9 +1478,15 @@
 			ПоместитьИсполнениеВПакет(ПакетХДТО, ВыборкаПериоды, ФабрикаСервиса);
 			
-			Ответ = Прокси.DownloadPosition(ПакетХДТО);	
+			// + FVasiliev IMDEV-8829 03.06.2026
+			//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+			Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);	
+			// - FVasiliev IMDEV-8829 03.06.2026
 		
 			ЕстьОшибкиПортфель = Ложь;
 			
-			Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+			// + FVasiliev IMDEV-8829 03.06.2026
+			//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+			Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелОбменаДляРегистрацииНенайденных);
+			// - FVasiliev IMDEV-8829 03.06.2026
 			Если Рез.ЕстьОшибки Тогда
 				ЕстьОшибкиПортфель = истина;
@@ -1405,5 +1510,9 @@
 		
 	КонецЦикла;
-	
+		
+		// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+		Вес = ИсполненияПоСделкам.Количество();
+		ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ОпубликоватьИсполнениеСделок_Сервер", ВремяНачала, Вес);
+		// - FVasiliev IMDEV-8972 24.06.2026
 
 КонецПроцедуры //ОпубликоватьИсполнениеСделок()
@@ -1736,7 +1845,13 @@
 	КонецЕсли;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
-	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
+	
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если ЗначениеЗаполнено(Рез.ТекстОшибки) Тогда
 		ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОшибки);
@@ -1830,11 +1945,17 @@
 	КонецЕсли;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
-	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
+	
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если ЗначениеЗаполнено(Рез.ТекстОшибки) Тогда
 		ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОшибки);
 	КонецЕсли;
-	
+
 КонецПроцедуры
 
@@ -1865,11 +1986,17 @@
 	КонецЕсли;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
-	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, Ложь, Неопределено);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
+	
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если ЗначениеЗаполнено(Рез.ТекстОшибки) Тогда
 		ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОшибки);
 	КонецЕсли;
-	
+
 КонецПроцедуры
 
@@ -1992,7 +2119,13 @@
 		КонецЦикла;	
 		
-		Ответ = Прокси.DownloadOrders(ПакетПорученияХДТО);
+		// + FVasiliev IMDEV-8829 03.06.2026
+		//Ответ = Прокси.DownloadOrders(ПакетПорученияХДТО);
+		Ответ = ВызватьDownloadOrders(Прокси, ПакетПорученияХДТО);
+		// - FVasiliev IMDEV-8829 03.06.2026
 					
-		Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+		// + FVasiliev IMDEV-8829 03.06.2026
+		//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+		Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+		// - FVasiliev IMDEV-8829 03.06.2026
 		Если Рез.ЕстьОшибки Тогда
 			ЕстьОшибки = истина;
@@ -2022,4 +2155,8 @@
 	Попытка
 		
+		// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+		ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+		// - FVasiliev IMDEV-8972 24.06.2026
+		
 		ПакетХДТО = ПроцедурыФункцииОбмена_МО_ФО.хдтоСформироватьПакет(ФабрикаСервиса);
 		
@@ -2028,7 +2165,13 @@
 		КонецЦикла;	
 		
-		Ответ = Прокси.DownloadPosition(ПакетХДТО);
+		// + FVasiliev IMDEV-8829 03.06.2026
+		//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+		Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+		// - FVasiliev IMDEV-8829 03.06.2026
 					
-		Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+		// + FVasiliev IMDEV-8829 03.06.2026
+		//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, Неопределено);
+		Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+		// - FVasiliev IMDEV-8829 03.06.2026
 		Если Рез.ЕстьОшибки Тогда
 			ЕстьОшибки = истина;
@@ -2044,4 +2187,9 @@
 			ТекстовыйЛог.ДобавитьСтроку(Рез.ТекстОтвета);
 		КонецЕсли;
+		
+		// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+		Вес = СделкиРЕПОМО.Количество();
+		ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ОпубликоватьСделкиРЕПОМО_Сервер", ВремяНачала, Вес);
+		// - FVasiliev IMDEV-8972 24.06.2026
 				
 	Исключение
@@ -2140,8 +2288,14 @@
 		Попытка
 			хдтоДобавитьВПакетУстановкаРейтингаПоЭмитенту(обПакет, Выборка.Ссылка, ФабрикаСервиса);	
+			// + FVasiliev IMDEV-8829 03.06.2026
 			//Ответ = Прокси.DownloadPosition(обПакет);
-			Ответ = Прокси.DownloadPosition(обПакет);
-			
-			Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка);
+			//Ответ = ВызватьDownloadPosition(Прокси, обПакет);
+			Ответ = ВызватьDownloadPosition(Прокси, обПакет);
+			// - FVasiliev IMDEV-8829 03.06.2026
+			
+			// + FVasiliev IMDEV-8829 03.06.2026
+			//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка);
+			Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса);
+			// - FVasiliev IMDEV-8829 03.06.2026
 			Если Рез.ЕстьОшибки Тогда
 				ЕстьОшибки = истина;
@@ -2306,5 +2460,6 @@
 	
 	Попытка
-		////СтруктураВозврата.ОпределениеWS = Новый WSОпределения("http://smsk02mg31u/AVC_UAT_FO/ws/avancore_ws.1cws?wsdl");
+		//СтруктураВозврата.ОпределениеWS = Новый WSОпределения("http://smsk02mg138u/AVC_UAT_FO/ws/avancore_ws.1cws?wsdl");
+		//Возврат СтруктураВозврата;
 		//Если ЭтотОбъект.АдресWS="http://smsk02mg138u/AVC_PP_FO/ws/avancore_ws.1cws?wsdl" Тогда
 		Если ЭтотОбъект.АдресWS="http://smsk02mg138u/AVC_PP_RETAIL_FO/ws/avancore_ws.1cws?wsdl" Тогда
@@ -2416,5 +2571,5 @@
 	
 	Пример = ПолучитьМакет("ПримерОтветаClearPosition");
-	Пример.Записать(ФайлПример, КодировкаТекста.UTF8);
+	Пример.Записать(ФайлПример, 8);
 	
 	Чтение	= Новый ЧтениеXML;
@@ -2437,5 +2592,5 @@
 	
 	Пример = ПолучитьМакет("ПримерОтветаPositionSet");
-	Пример.Записать(ФайлПример, КодировкаТекста.UTF8);
+	Пример.Записать(ФайлПример, 8);
 	
 	Чтение	= Новый ЧтениеXML;
@@ -2464,4 +2619,8 @@
 Функция РЕПОДенежныеСредстваНаДату(Дата, Отбор) Экспорт
 	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 	Условие = "";
 	ДатаЗаполнения =  НачалоДня(Дата);
@@ -2545,4 +2704,9 @@
 		
 	КонецЦикла;
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	Вес = ВыборкаСделкаРЕПО.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_РЕПОДенежныеСредстваНаДату_Сервер", ВремяНачала, Вес);
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 	Возврат ПозицияРЕПОФО;
@@ -3437,4 +3601,8 @@
 Функция ЦенныеБумагиНаДату(Дата, Отбор = Неопределено) Экспорт
 	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 	Условие = "";
 	УсловиеМандат = "";
@@ -3472,9 +3640,19 @@
 	ОбщегоНазначенияМО.ДобавитьВЛогОтладки(Запрос, "Регистр ""Ценные бумаги"": Ценные бумаги на дату");
 	
-	Возврат Запрос.Выполнить().Выгрузить();
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ТЗЦенныеБумаги = Запрос.Выполнить().Выгрузить();
+	Вес = ТЗЦенныеБумаги.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ЦенныеБумаги_Сервер", ВремяНачала, Вес);
+	
+	Возврат ТЗЦенныеБумаги;
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 КонецФункции // ДоходыКПолучениюНаДату()
 
 Функция ДенежныеСредстваНаДату(Дата, Отбор = Неопределено) Экспорт
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 	Условие = "";
@@ -3607,9 +3785,19 @@
 	ОбщегоНазначенияМО.ДобавитьВЛогОтладки(Запрос, "Регистр ""Денежные средства"": Денежные средства");
 	
-	Возврат Запрос.Выполнить().Выгрузить();
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ТЗДенежныеСредства = Запрос.Выполнить().Выгрузить();
+	Вес = ТЗДенежныеСредства.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ДенежныеСредства_Сервер", ВремяНачала, Вес);
+	
+	Возврат ТЗДенежныеСредства;
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 КонецФункции // ДоходыКПолучениюНаДату()
 
 Функция БлокировкиНаДату(Дата, Отбор = Неопределено) Экспорт
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
 
 	Условие = "";
@@ -3665,9 +3853,19 @@
 	ОбщегоНазначенияМО.ДобавитьВЛогОтладки(Запрос, "Регистр ""Денежные средства"": Денежные средства");
 	
-	Возврат Запрос.Выполнить().Выгрузить();
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ТЗБлокировки = Запрос.Выполнить().Выгрузить();
+	Вес = ТЗБлокировки.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_Блокировки_Сервер", ВремяНачала, Вес);
+	
+	Возврат ТЗБлокировки;
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 КонецФункции // ДоходыКПолучениюНаДату()
 
 Функция СЧА_РСА_НаДату(Дата, Отбор = Неопределено) Экспорт
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
 
 	Условие = "";
@@ -3746,5 +3944,11 @@
 	ОбщегоНазначенияМО.ДобавитьВЛогОтладки(Запрос, "Регистр ""Денежные средства"": Денежные средства");
 	
-	Возврат Запрос.Выполнить().Выгрузить();
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ТЗСЧА_РСА = Запрос.Выполнить().Выгрузить();
+	Вес = ТЗСЧА_РСА.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_СЧА_РСА_Сервер", ВремяНачала, Вес);
+	
+	Возврат ТЗСЧА_РСА;
+	// - FVasiliev IMDEV-8972 24.06.2026
 	
 КонецФункции // ДоходыКПолучениюНаДату()
@@ -4259,4 +4463,9 @@
 		Возврат ВыборкаДетальныеЗаписи.СчетМандата;
 	Иначе
+		// IMAPPS-37860++
+		Если НЕ СтроковыеФункцииКлиентСервер.ЭтоУникальныйИдентификатор(КодСчета) Тогда
+			Возврат Справочники.СчетаМандатов.ПустаяСсылка();
+		КонецЕсли;
+		// IMAPPS-37860--
 		УИД = Новый УникальныйИдентификатор(КодСчета); 
 		СсылкаСчетМанадат = Справочники.СчетаМандатов.ПолучитьСсылку(УИД);
@@ -4632,4 +4841,8 @@
 Функция ИсполнениеСделок(Дата, Отбор = Неопределено) Экспорт 
 	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 	Если ИсполнениеПоСуммеМО Тогда
 		СписокПозиций = ПолучитьПозициюФО(Дата, "Плановая");
@@ -4713,5 +4926,10 @@
 		     
 	КонецЕсли;
-    
+	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	Вес = ИсполненныеСделкиМО.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_ИсполнениеСделок_Сервер", ВремяНачала, Вес);
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 	Возврат ИсполненныеСделкиМО;
 
@@ -4863,9 +5081,15 @@
 	КонецЕсли;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	
 	ЕстьОшибкиПортфель = Ложь;
 	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелОбменаДляРегистрацииНенайденных, УзелОбменаДляРегистрацииНенайденныхСчетовМандатов);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если Рез.ЕстьОшибки Тогда
 		ЕстьОшибки = Истина;
@@ -5442,5 +5666,51 @@
 #КонецОбласти
 
+// FVasiliev IMDEV-8829 03.06.2026
+Функция ВызватьDownloadPosition(Прокси, ПакетХДТО) Экспорт
+	
+	Если РежимОтладкиСервиса Тогда
+		Возврат Неопределено;
+	КонецЕсли;
+	
+	Возврат Прокси.DownloadPosition(ПакетХДТО);
+	
+КонецФункции
+
+// FVasiliev IMDEV-8829 03.06.2026
+Функция ВызватьDownloadOrders(Прокси, ПакетХДТО) Экспорт
+	
+	Если РежимОтладкиСервиса Тогда
+		Возврат Неопределено;
+	КонецЕсли;
+	
+	Возврат Прокси.DownloadOrders(ПакетХДТО);
+	
+КонецФункции
+
+// FVasiliev IMDEV-8829 03.06.2026
+Функция ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелНенайденных = Неопределено, УзелСчетовМандатов = Неопределено) Экспорт
+	
+	Если РежимОтладкиСервиса Тогда
+		Возврат Новый Структура(
+			"ЕстьОшибки, ЕстьПредупреждения, ТекстОшибки, ТекстОшибкиСервиса"
+			+ ", МассивНенайденныхАктивов, МассивНенайденныхСчетовМандатов"
+			+ ", МассивНенайденныхКодовКонтрагентов, МассивНенайденныхКодовПортфелей"
+			+ ", МассивНенайденныхКодовВалюты, ТекстОтвета"
+			, Ложь, Ложь, "", ""
+			, Новый Массив, Новый Массив, Новый Массив, Новый Массив, Новый Массив
+			, "");
+	КонецЕсли;
+	
+	Возврат ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелНенайденных, УзелСчетовМандатов);
+	
+КонецФункции
+
+// + FVasiliev IMDEV-8829 03.06.2026
+РежимОтладкиСервиса = Ложь; // Истина — пропускать вызовы сервиса (для отладки, включается "заглушка")
+// - FVasiliev IMDEV-8829 03.06.2026
+вклОтладка = Ложь; // Истина - Включается логирование результатов
+
 ПространствоИменСервиса = "http://avancore.ru/xsd";
+
 ЕстьОшибки = Ложь;
 МассивФайлов = Новый Массив;
@@ -5449,8 +5719,11 @@
 ОписаниеОшибки = "";
 КешБазыПИФ = Новый Соответствие;
-вклОтладка = Ложь;
 
 ПроцедурыФункцииОбмена_МО_ФО = Обработки.внПроцедурыИФункцииОбменаСФронтОфис.Создать();
 ВыгружатьАктивыСчета = Ложь;
+
+// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+ВесКоличествоМандатов = 0;
+// - FVasiliev IMDEV-8972 24.06.2026
 
 //IMAPPS-19973++