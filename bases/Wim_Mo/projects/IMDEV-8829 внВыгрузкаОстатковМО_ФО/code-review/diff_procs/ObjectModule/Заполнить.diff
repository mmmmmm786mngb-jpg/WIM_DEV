--- A:Заполнить
+++ F:Заполнить
@@ -1,4 +1,9 @@
 Процедура Заполнить(Отбор, ДатаЗаполнения) Экспорт
+	
+	// + FVasiliev IMDEV-8972 09.06.2026 Оценка производительности
+	ВремяНачала = ОценкаПроизводительности.НачатьЗамерВремени();
+	// - FVasiliev IMDEV-8972 09.06.2026
+	
 	// IMDEV-8829 ++ в пачечном режиме список мандатов задан координатором, не перезаполняем
 	Если ОтборМандат И НЕ ЗначениеЗаполнено(ИдентификаторСеансаОбмена) Тогда
 		ЗаполнитьСписокМандатДляОтбора();	
@@ -41,5 +46,12 @@
 		
 	КонецЦикла;
 	
+	// + FVasiliev IMDEV-8972 24.06.2026 Оценка производительности
+	//Вес = РЕПОДеньги.Количество() + ЦенныеБумаги.Количество() + ДенежныеСредства.Количество() 
+	//+ СЧА_РСА.Количество() + Блокировки.Количество() + ИсполненияПоСделкам.Количество();
+	ВесКоличествоМандатов = Отбор.ОтборМандаты.Количество();
+	ОценкаПроизводительности.ЗакончитьЗамерВремени("внВыгрузкаОстатковМО_ФО_Заполнить_Сервер", ВремяНачала, ВесКоличествоМандатов);
+	// - FVasiliev IMDEV-8972 24.06.2026
+	
 КонецПроцедуры // Заполнить()
 