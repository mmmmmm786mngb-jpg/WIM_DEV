--- A:ОпубликоватьАктивыРЕПО
+++ F:ОпубликоватьАктивыРЕПО
@@ -32,11 +32,17 @@
 		КонецЕсли;
 	КонецЦикла;
 	
-	Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Ответ = Прокси.DownloadPosition(ПакетХДТО);
+	Ответ = ВызватьDownloadPosition(Прокси, ПакетХДТО);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	
 	ЕстьОшибкиПортфель = Ложь;
 	
-	Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+	// + FVasiliev IMDEV-8829 03.06.2026
+	//Рез = ПроцедурыФункцииОбмена_МО_ФО.ОбработатьОтвет(Ответ, ФабрикаСервиса, вклОтладка, УзелОбменаДляРегистрацииНенайденных);
+	Рез = ОбработатьОтветЧерезОбертку(Ответ, ФабрикаСервиса, УзелОбменаДляРегистрацииНенайденных);
+	// - FVasiliev IMDEV-8829 03.06.2026
 	Если Рез.ЕстьОшибки Тогда
 		ЕстьОшибкиПортфель = Истина;
 		ЕстьОшибки = Истина;