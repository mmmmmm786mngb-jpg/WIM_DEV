--- A:ЗаписатьДанные
+++ F:ЗаписатьДанные
@@ -28,7 +28,12 @@
 								"Подробности смотрите по источнику обмена (ВыгрузкаОстатков_ФО) -> Перейти к бизнес-процессам";
 		КонецЕсли;
 		
-		РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		// + FVasiliev IMDEV-8829 15.07.2026
+		// РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		Если НЕ ЗначениеЗаполнено(ИдентификаторСеансаОбмена) Тогда
+			РассылкаСообщения("Загрузка остатков МО -> ФО", ТекстОповещения, ЕстьОшибки, ЕстьИсключения);
+		КонецЕсли;
+		// - FVasiliev IMDEV-8829 15.07.2026
 		
 	Исключение
 	КонецПопытки;
@@ -55,10 +60,12 @@
 #Область ЭкспортныеПроцедурыИФункции
 
 // IMDEV-8829 ++
-// Описание: Записывает в журнал регистрации все параметры вызова фонового задания воркера.
+// Описание: Координатор параллельной выгрузки остатков МО -> ФО пачками мандатов.
+//   Делит все мандаты с остатками на N непересекающихся пачек, запускает по одному
+//   фоновому заданию на пачку (штатная команда "ФоноваяВыгрузка"), дожидается завершения
+//   всех заданий (барьер) и один раз выполняет финальную рассылку по перерасчёту РСА.
 //
 // Параметры:
-//  ПараметрыМетода - Массив - аргументы для ДополнительныеОтчетыИОбработки.ВыполнитьКоманду.
-//  НомерПачки      - Число  - индекс пачки (0-based).
-//  СессияОбмена    - Строка - общий идентификатор сеанса обмена.
+//  ПараметрыВыполненияКоманды - Структура - параметры команды БСП (содержат
+//      ДополнительнаяОбработкаСсылка, ПараметрыДополнительнойОбработки, Узел).
 //